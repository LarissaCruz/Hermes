import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
   placeholderStyles: {
    color: "black",
  },
   dropdown: {
    borderColor:"#9999",
    borderWidth:0,
    borderBottomWidth:1, 
    paddingHorizontal:0, 
    borderRadius:0,
    marginBottom:10
  },
   dropdownGender: {
    
  },
});
