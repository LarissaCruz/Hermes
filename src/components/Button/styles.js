import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    backgroundColor: "black",
    paddingVertical: 18,
    marginRight: 10,
    marginTop: 20,
    borderRadius: 5,
  },
  label: {
    color: "#fff",
    textAlign: "center",
    fontSize: 14,
    fontWeight: "700",
  },
});
