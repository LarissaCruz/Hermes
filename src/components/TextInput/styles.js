import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    minHeight: 40,
    marginBottom: 10,
    borderBottomWidth: 1,
    borderColor: "#9999",
    paddingVertical: 10,
    fontSize: 16,
  },
});
